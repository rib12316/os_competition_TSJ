# agent-mem 容器镜像（占位）
# 国产化部署优先使用官方镜像：
#   - openEuler: openeuler/vllm-cpu  或  quay.io/ascend/vllm-ascend:v0.22.1rc1-openeuler
# 本 Dockerfile 作为后续自定义构建的起点（F9 · 缝G）。
FROM ubuntu:22.04

# TODO(F9): 按 dev-guide/environment-setup.md 安装 uv + agent-mem + 推理后端
# WORKDIR /workspace
# COPY . .
WORKDIR /workspace
CMD ["bash"]
